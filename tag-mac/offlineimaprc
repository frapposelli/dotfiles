[general]
accounts = VMware
maxsyncaccounts = 1

[Account VMware]
localrepository = LocalVMware
remoterepository = RemoteVMware

[Repository LocalVMware]
type = Maildir
sync_deletes = no
localfolders = ~/Mail/VMware

[Repository RemoteVMware]
type = IMAP
ssl = no
auth_mechanisms = LOGIN
remotehost = localhost
remoteport = 1143
remoteuser = frapposelli@vmware.com
remotepass = xxx
maxconnections = 1
readonly = true
sync_deletes = no
#
# instead of filtering for what we want, we filter out the unwanted
#
# folderfilter = lambda folder: folder in [ 'Sent', 'INBOX', 'Archive' ]
#
folderfilter = lambda folder: folder not in [ 'Calendar', 'Calendar/Birthdays', 'Calendar/Sub Folder 1', 'Calendar/Sub Folder 2', 'Calendar/United States holidays', 'Contacts', 'Contacts/Sub Folder 1', 'Contacts/Sub Folder 2', 'Contacts/Skype for Business Contacts', 'Deleted Items', 'Drafts', 'Journal', 'Junk Email', 'Notes', 'Outbox', 'Sync Issues', 'Sync Issues/Conflicts', 'Sync Issues/Local Failures', 'Sync Issues/Server Failures', 'Tasks', 'Tasks/Sub Folder 1', 'Tasks/Sub Folder 2', 'Trash' ]

